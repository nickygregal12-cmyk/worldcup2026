# Non-executable database designs

Files in this directory are review material only. They are not migrations and
must not be passed to `supabase db push`.

Only files under `supabase/migrations/` form the active migration history.
After a design is approved, create a new timestamped migration from the
approved decisions rather than moving or renaming a design file.

# Euro 2028 Predictor Agent Start

Future agents must read these first, in order:

1. `AGENT-CONTROL/00-READ-FIRST.md`
2. `PROJECT-CONTROL.md`
3. the current user task
4. source documents only when the task requires them

Stop if instructions conflict. Do not improvise around safety rules.

Do not commit unless Nicky explicitly approves it. Do not use `git add .`.

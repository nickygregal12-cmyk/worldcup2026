# CONTRACTS-REF-LOCKED-SURFACES-3

## Status

Next recording batch.

## Purpose

Record the v9 locked-design and planning pack into the repo before implementation.

This is documentation/reference only.

## Include

- v6/v9 locked design documents;
- Home + Missing Surfaces build brief;
- Candidate Team Pool brief;
- Admin Scenario Runner brief;
- Simulation Safety Guidelines;
- Streamlined Remaining Jobs Plan;
- Scoring lock notes;
- updated Decision Register / Roadmap / Ledger / Charter drafts;
- final-design-sweep checklist;
- approved reference files;
- missing-surface records.

## Do not include

- product implementation;
- source route changes;
- Supabase changes;
- migrations;
- scoring logic changes;
- fake result writes;
- official result-entry changes.

## Acceptance

The recording batch is complete only if:

- approved HTML contracts are installed according to the corrected one-contract-per-surface map;
- no duplicate reference files are created for the same surface;
- v7 streamlined batch order is recorded;
- candidate-team and scenario-runner briefs are recorded as future stages only;
- no `src/`, Supabase, migration, scoring, resolver, Auth or audit-logic implementation is changed unless explicitly scoped;
- existing gates pass.

## v8 additional recording requirement

Record the unresolved predicted group tiebreaker prompt decision.

This is documentation/reference only in the recording batch.

Do not implement the resolver or prompt in the recording commit.

## v9 additional recording requirement

Record the Edge-Case Rules Addendum.

This is documentation/reference only in the recording batch.

Do not implement these edge-case rules in the recording commit.
